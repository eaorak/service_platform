package com.adenon.sp.channels.config;

import com.adenon.sp.administration.ConfigLocation;
import com.adenon.sp.administration.annotate.MBean;

@MBean(location = ConfigLocation.CHANNELS, subLocation = "channels=Manager")
public class ChannelsConfiguration {

    public ChannelsConfiguration() {
    }

}
