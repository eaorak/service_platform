package com.adenon.channel.sss.api.message;

import com.adenon.sp.kernel.event.message.IMessageTypes;

public enum SmsMessageTypes implements IMessageTypes {

    SMS_MESSAGE_RECEIVED(1), //
    SMS_MESSAGE_DELIVER(2), //
    SMS_MESSAGE_SEND(3), //
    SMS_MESSAGE_SEND_ACK(4);

    private int id;

    private SmsMessageTypes(int id) {
        this.id = id;
    }

    public int id() {
        return this.id;
    }

}
